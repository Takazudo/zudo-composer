// Shared by release identity and package conformance. Only these published
// entries attest the contract; checkout sources, tooling and README do not.
export const CONTRACT_IDENTITY_ENTRIES = Object.freeze(["dist", "package.json"]);
