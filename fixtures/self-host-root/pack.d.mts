// Ambient type for `pack.mjs`, which stays plain JavaScript on purpose (see
// the comment in that file). Its shape is asserted, not declared here — every
// test that imports it casts to `TrustedComponentPack` itself.
export const componentPack: unknown;
